## Summary

<!-- What changed and why. A few bullets — lead with intent, not a file list. -->

-

## Test plan

<!-- Check off what you actually ran. CI covers build/unit-tests/e2e on every
     PR (see .github/workflows/ci.yml) — this is for anything beyond that,
     especially UI behavior CI can't see. -->

- [ ] `cargo test --features ssr` passes
- [ ] `cargo leptos end-to-end` passes (add/update specs in `end2end/tests/` if this touches user-facing behavior)
- [ ]

## Related

<!-- Delete if not applicable. -->

- TDD.md phase/section:
- Follow-up work this surfaces (if any):
